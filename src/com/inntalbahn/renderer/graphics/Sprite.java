/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */

package com.inntalbahn.renderer.graphics;

/**
 *
 * @author Usr
 */
public class Sprite {
/*
    public final int SIZE;
    //private int x, y;
    public int[] pixels;
    private SpriteSheet sheet;
    public int animTime = 1;
    private int tilesize = 0;
    private int alpha = 0;
    private int currentPixel = 0;
    
    public static Sprite worm = new Sprite(64, 0, 0, 60, -8355648,SpriteSheet.tiles);  //Create Sprite-Object
    //public static Sprite bg = new Sprite(256, 0, 0, 256, 0,SpriteSheet.bgsheet);  //Create Sprite-Object
    
    public Sprite(int size, int x, int y, int tilesize, int alpha,SpriteSheet sheet){
        SIZE = size;
        pixels = new int[size*size];
        //this.x = x * tilesize;
        //this.y = y * tilesize;
        this.tilesize = tilesize;
        this.sheet = sheet;
        if(alpha != 0) this.alpha=alpha;
        load();
    }
    
    public void load(){
        for (int y = 0; y < SIZE; y++){
            for (int x = 0; x < SIZE; x++){
                currentPixel = (x) + (y + animTime*tilesize) * sheet.SIZEX;
                pixels[x+y*SIZE] = sheet.pixels[currentPixel];
            }
        }
    }
    */
}
